


interface ProjectProps {
    expanded: boolean;
    onChange: (event: React.SyntheticEvent, isExpanded: boolean) => void;
  }
  
  const Project: React.FC<ProjectProps> = ({ expanded, onChange }) => {
  
  
   
    return (
      <>
       
      </>
  
    )
  }
  export default Project;